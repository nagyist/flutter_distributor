# Test fixture for the Xcode Builder integration tests in
# crates/app_builder/tests/xcode_builder.rs.
#
# Static reference only: Phase 1 tests call the builder crate API directly, not
# `fastforge workflow run`.
name: Xcode Builder macOS smoke package

on:
  workflow_dispatch:

jobs:
  zip:
    name: macOS ZIP
    steps:
      - name: Package ZIP
        uses: fastforge/package@v1
        with:
          platform: macos
          target: zip
          output: dist/
          build-args: >-
            {"project":"NativeMacOSFixture.xcodeproj","scheme":"NativeMacOSFixture",
            "configuration":"Release","derived-data-path":"build"}
