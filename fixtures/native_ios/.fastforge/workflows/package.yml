# Test fixture for the Xcode Builder integration tests in
# crates/app_builder/tests/xcode_builder.rs.
#
# Static reference only: Phase 1 tests call the builder crate API directly, not
# `fastforge workflow run`.
name: Xcode Builder iOS smoke package

on:
  workflow_dispatch:

jobs:
  ipa:
    name: iOS IPA
    steps:
      - name: Package IPA
        uses: fastforge/package@v1
        with:
          platform: ios
          target: ipa
          output: dist/
          build-args: >-
            {"project":"NativeIOSFixture.xcodeproj","scheme":"NativeIOSFixture",
            "configuration":"Release","export-method":"debugging"}
